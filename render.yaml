services:
  - type: web
    name: cat-dog-classifier-api
    env: python
    rootDir: exercise_1/backend
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: MODEL_NAME
        value: vit_small_patch16_224
      - key: CHECKPOINT_PATH
        value: ../models_checkpoint/vit_small_head.pt
      - key: CORS_ORIGINS
        value: "*"
